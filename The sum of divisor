#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
	int num, sum;
	
	scanf("%d", &num);
	sum = 0;
	for (int i = 1; i <= num; i++) {
		if (num%i == 0)
			sum += i;
	}
	printf("result : %d\n", sum);
	return 0;
}
